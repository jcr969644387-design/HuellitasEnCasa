<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        HuellitasEnCasa es una aplicación 100% offline.
        No se declara el permiso INTERNET a propósito: no hay backend,
        analítica, anuncios ni comunicación en red de ningún tipo.
        No se solicitan datos personales, ubicación, contactos, cámara
        ni micrófono porque ninguna función de la app los requiere.
    -->

    <application
        android:name=".HuellitasApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.HuellitasEnCasa">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HuellitasEnCasa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
